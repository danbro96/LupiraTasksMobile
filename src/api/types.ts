// Hand-written API types live here until the Orval-generated client (under
// `src/api/generated`) supersedes them. Kept as a placeholder so imports from
// `../api/types` resolve while the backend OpenAPI contract is still being
// shaped.

export type Empty = Record<string, never>;
