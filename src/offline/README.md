# offline/

Reserved for the offline layer (local SQLite store, network-state-aware
outbox, and background sync). Not yet implemented — the dependencies
(`expo-sqlite`, `@react-native-community/netinfo`, `uuid`) are installed and
this folder is staged so the work lands cleanly in a later phase.
